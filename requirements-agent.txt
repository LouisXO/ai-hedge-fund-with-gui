# Extra deps for agent/ and hedge_fund/validation, installed into ~/.hedgefund-venv
# on top of `poetry install` (kept out of upstream's pyproject):
#   ~/.hedgefund-venv/bin/python -m pip install -r requirements-agent.txt
duckdb==1.5.5        # same version as optradar's ~/.moomoo/venv: one storage format, no lock-out
yfinance==1.7.0      # research history only; fetch once (Yahoo bans bulk re-downloads)
moomoo-api==10.10.7008
