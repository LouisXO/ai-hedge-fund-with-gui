# ~/.hedgefund-venv (Python 3.13). Replaces upstream's pyproject/poetry.lock (removed 2026-09-23).
#   python3.13 -m venv ~/.hedgefund-venv && ~/.hedgefund-venv/bin/python -m pip install -r requirements.txt
pandas==2.3.3
numpy==1.26.4
scipy==1.17.1
pydantic==2.13.4
python-dotenv==1.0.0
requests==2.34.2
PyYAML==6.0.3
matplotlib==3.11.1
pytest==7.4.4
duckdb==1.5.5        # same version as optradar's ~/.moomoo/venv: one storage format, no lock-out
yfinance==1.7.0      # research history only; fetch once (Yahoo bans bulk re-downloads)
moomoo-api==10.10.7008
